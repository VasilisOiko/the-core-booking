import type { ThemeConfig } from 'antd';

const theme: ThemeConfig = {
    token: {
        fontSize: 16,
        colorPrimary: '#1890ff',
    },
    components: {
        Layout: {
            bodyBg: '#252525',
            // headerBg: '#484556',
        },
    },
};

export default theme;